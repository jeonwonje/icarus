{
  "name": "icarus",
  "version": "0.2.0",
  "description": "Claude Code skill library for a OneDrive life-hub. Each data source (Canvas, …) is a skill that syncs into <domain>/<subject>/{canvas,user}.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "telegram": "^2.26.0",
    "input": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
